import 'package:flutter/material.dart';

class wastebasket extends  StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("waste basket"),),
    body:null,
    
    );
  }
}